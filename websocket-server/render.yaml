services:
  - type: web
    name: liar-websocket
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://votre-app.vercel.app
